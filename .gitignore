# Ignore secrets directory (contains keys, DBs, audit logs)
server/Web/secrets
!server/Web/secrets/samples

# Editor and system files
.DS_Store
Thumbs.db
*.pyc
__pycache__/

# Env files
.env

# To avoid possible data leakage for now:
scripts/
# Scripts are currently isn't updated to the latest code, so they doesn't work. WIP.
.cache/
.backup/

.vscode/

# not sure
.venv/

# Node / Tailwind build tooling
node_modules/
package-lock.json

# Built CSS is committed (it's the deployment artifact)
# server/TestWeb/fluxdrop_pp/styles.css  ← do NOT ignore this

# Local sync variables are excluded
deploy.env

# this file is used for applying patches and can be safely ignored
temp.py