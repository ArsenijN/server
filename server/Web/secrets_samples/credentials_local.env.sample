# Local credential store for environment variables that shouldn't be
# checked into version control.  Copy this to ../credentials_local.env and
# add any secret key=value pairs you need.

# Example:
# API_KEY=abcd1234
# OTHER_SECRET=foo
