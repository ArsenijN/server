.backup/
src/src.zip

# Ignore secrets directory (contains keys, DBs, audit logs)
server/Web/secrets
!server/Web/secrets/samples

# Editor and system files
.DS_Store
Thumbs.db
*.pyc
__pycache__/

# Env files
.env

# To avoid possible data leakage for now:
scripts/
.cache/
services/

.vscode/

# Temporarely keep FluxDrop out of scope due to some quality reasons
src/FluxDrop site/

# not sure
.venv/

# Node / Tailwind build tooling
node_modules/
package-lock.json

# Tailwind source (not needed on server)
# src/input.css

# Built CSS is committed (it's the deployment artifact)
# server/TestWeb/fluxdrop_pp/styles.css  ← do NOT ignore this