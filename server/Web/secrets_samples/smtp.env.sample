# Sample SMTP configuration for sending verification emails.
# Copy this file to ../smtp.env and fill in real values.

# SMTP_SERVER=smtp.example.com
# SMTP_PORT=587
# SMTP_SENDER_EMAIL=you@example.com
# SMTP_SENDER_PASSWORD=your-app-password
